// Runtime config for the static SPA. Replaced at deploy time on Vercel via
// a build step (or hand-edited if you prefer). Empty string = call the same
// origin that served the page (HF Spaces single-container dev mode).
window.POINTSX_API_BASE = "";
